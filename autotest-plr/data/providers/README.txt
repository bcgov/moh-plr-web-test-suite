PROVIDER TEST DATA REQUIREMENTS
===============================

default:
    Default provider, maximum data:
        - active, history and audit data blocks present for every section
    Multiple active/inactive blocks with varying "Effective From" dates
    for testing data block sort order.

minimum:
    Minimum data, i.e. added during provider add.
